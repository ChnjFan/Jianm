#include <gtest/gtest.h>

// Temporary smoke test — verifies the project compiles and links.
// Replace with real tests as the codebase grows.
TEST(SmokeTest, ProjectLinks) {
    EXPECT_TRUE(true);
}
